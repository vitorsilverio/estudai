/**
 * Emails allowed to use the redação (essay) feature. Deliberately not automatic/AI-driven —
 * corrections are done manually on request — so access stays limited to these two people
 * instead of being open to any signed-in user.
 */
export const ALLOWED_ESSAY_EMAILS = ['vitor.silverio.rodrigues@gmail.com', 'mariaramos2413@gmail.com'];
